# Que un Node incompatible corte el install en vez de dejar un warning que se
# pierde en el scroll. @stellar/stellar-sdk pide >=22.12.0 y en Node 20 el SDK
# entero queda fuera de soporte, que es medio proyecto.
engine-strict=true
