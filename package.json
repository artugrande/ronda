{
  "name": "ronda",
  "version": "0.1.0",
  "description": "Ahorro rotativo multi-cadena en USDT0 sobre Stellar",
  "private": true,
  "license": "ISC",
  "scripts": {
    "test": "cargo test",
    "build": "stellar contract build",
    "deploy:testnet": "scripts/deploy.sh testnet"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.0.1"
  }
}
